import NotesSubmitForm from "@/components/root/notes-submit-form";

const SubmitPage = () => {
  return (
    <section className="max-container p-6 min-h-[calc(100vh-8rem)]">
      <NotesSubmitForm />
    </section>
  );
};

export default SubmitPage;
